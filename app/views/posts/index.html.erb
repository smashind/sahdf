<div class="row text-center">
  <div class="col-sm-10 col-sm-offset-1">
    <header>
	    <h1 class="home">Stay at Home Dad Funny</h1>
	    <p>Funny stories from stay at home dads. Got a funny story that happened while taking care of your son or daughter? Post it here! No story is too crazy to share.</p>
	    <%= link_to "Post Your Sad Funny", "#", data: { toggle: "modal", target: "#navbar_post" }, class: "btn btn-success btn-lg new-post-home" %>
    </header>
  </div>
</div>
<div class="row">
  <div class="col-lg-8">
    <div id="posts">
			<% @posts.each do |post| %>

				<%= post_style(post, current_user || User.new) %>
				
			<% end %>
		</div>
		<%= will_paginate @posts, renderer: BootstrapPagination::Rails %>
	</div>
	<div class="col-lg-4">
	  <div class="social-ads social-ads-homepage">
	    <%= render partial: "layouts/fb_sidebar" %>
	    <%= render partial: "layouts/twitter_sidebar" %>
	    <%= render partial: "layouts/ads" %>
    </div>
	</div>
</div>