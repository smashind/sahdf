$('#post_body').val("");
sweetAlert("Nice!", "Your sad funny was saved.", "success");
$('#post-form').slideToggle();

// Old method for adding posts, doesn't play well with pagination
// and would allow all the spam to get through to the front page

// $('#posts').prepend("<%= j render @post %>");
// $('#post_body').val("");